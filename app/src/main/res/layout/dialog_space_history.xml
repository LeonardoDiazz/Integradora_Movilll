<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Chip nombre del espacio -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="20dp"
        android:paddingTop="4dp"
        android:paddingBottom="12dp">

        <TextView
            android:id="@+id/tvHistorySpaceName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="12dp"
            android:paddingVertical="5dp"
            android:text="🏢 ESPACIO"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="#0D9488"
            android:background="@drawable/bg_chip_teal"/>
    </LinearLayout>

    <!-- Separador -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#F3F4F6"/>

    <!-- Contenido: lista o estado vacío -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="160dp">

        <!-- Estado vacío -->
        <LinearLayout
            android:id="@+id/layoutEmpty"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="32dp"
            android:visibility="gone">

            <TextView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:text="ℹ"
                android:textSize="28sp"
                android:textColor="#D1D5DB"
                android:gravity="center"
                android:layout_marginBottom="12dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Este espacio no tiene registros de reservas previas."
                android:textSize="13sp"
                android:textColor="#9CA3AF"
                android:gravity="center"/>
        </LinearLayout>

        <!-- Lista de historial -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvHistory"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxHeight="240dp"
            android:padding="12dp"
            android:clipToPadding="false"
            android:visibility="gone"/>

    </FrameLayout>

    <!-- Separador -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#F3F4F6"/>

    <!-- Footer -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ÚLTIMOS 10 REGISTROS"
        android:textSize="11sp"
        android:textStyle="bold"
        android:textColor="#9CA3AF"
        android:gravity="center"
        android:paddingVertical="14dp"
        android:letterSpacing="0.08"/>

</LinearLayout>
